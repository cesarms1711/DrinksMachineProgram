@using DrinksMachineProgram.Entities
@model List<Coin>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var coin in Model)
        {
            <tr scope="row">
                <td class="text-center">@coin.Id</td>
                <td>@coin.Name</td>
                <td class="text-center">
                    <button type="button" class="btn btn-primary btn-sm" onclick="dataForm.detail(@coin.Id);">
                        <i id="btnBorrar" class="fa fa fa-search"></i> Detail
                    </button>
                    <button type="button" class="btn btn-secondary btn-sm" onclick="dataForm.edit(@coin.Id);">
                        <i id="btnBorrar" class="fa fa fa-edit"></i> Edit
                    </button>
                    <button type="button" class="btn btn-danger btn-sm" onclick="dataForm.delete(@coin.Id);">
                        <i id="btnBorrar" class="fa fa fa-trash"></i> Delete
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>